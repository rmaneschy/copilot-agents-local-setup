# Scripts Legados

Os scripts nesta pasta são **obsoletos** e mantidos apenas para referência histórica. Não devem ser utilizados em novas instalações.

| Script | Substituído por | Motivo da Deprecação |
|:---|:---|:---|
| `setup.ps1` | `setup-codebase-memory.ps1` + `setup-serena.ps1` | Instalava Ollama + mcp-vector-search (v2.x). Stack substituída. |
| `setup-vector-search.ps1` | `setup-codebase-memory.ps1` | mcp-vector-search substituído por codebase-memory-mcp. |
| `setup-alternative-node.ps1` | `setup-codebase-memory.ps1` | Alternativa Node.js do mcp-vector-search. Não mais necessária. |

Para a instalação atual, consulte o [README principal](../../README.md).
